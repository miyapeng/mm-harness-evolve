# Run artifacts

Bulk artifacts are excluded from Git. A run contains resolved configuration, event JSONL,
task attempts, raw requests/responses, media, content-addressed harness versions, proposal
patches and selection records. Publish experiment indexes and checksums in experiments/.

